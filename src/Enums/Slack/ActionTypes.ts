/**
 * Types of attachment we can use
 *
 * {@see https://api.slack.com/docs/interactive-message-field-guide#message_action_fields}
 */
export enum ActionTypes {
    BUTTON = 'button',
    SELECT = 'select'
}
