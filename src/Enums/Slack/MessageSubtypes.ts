/**
 * Slack subtypes for message events
 *
 * {@see https://api.slack.com/events/message#message_subtypes}
 */
export enum MessageSubtypes {
    BOT_MESSAGE = 'bot_message',

    // There are loads more, but only the above is required for this app.
}
